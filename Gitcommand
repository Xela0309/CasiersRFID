// Commande git //

// Ne pas oublier la mise à jour avant d'ajouter

git pull

// Connaitre sa branche actuelle

git branch

// Changer de branche

git switch NomDeLaBranche

// Pour Envoyer les fichiers

git add FichierAChoisir

git commit FichierAChoisir -m "Message(Ne pas oublier les doubles quotes)"

git push

// Si vous avez un probleme appellez moi (fetch par exemple)
